# Vercel preview deployments: leave APP_BASE_URL unset so the Auth0 v4 SDK
# infers the correct base URL from the request's Host header at runtime.
# Production deployments should set APP_BASE_URL in the Vercel UI.
#
# NOTE: The v4 SDK uses APP_BASE_URL (not AUTH0_BASE_URL).
